apiVersion: v1
kind: Service
metadata:
  name: xygjbij
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "xygjbij"
    port: 8080
  selector:
    app: xygjbij
